function sticky() {
    $(".header").sticky()
}

function setImageEffects() {
    $(".zoomedImageFrame").hover(function() {
        $(this).find("img.previewImg")[0];
        $(this).addClass("imageZoom")
    }, function() {
        $(this).find("img.previewImg")[0];
        $(this).removeClass("imageZoom")
    })
}

function setMobileArrow(i) {
    var n = i - 40,
        t = $("#mobileArrow");
    t && (t.css("top", n + "px"), $(window).width() < 768 && $(window).scroll(function() {
        $(this).scrollTop() > 100 ? t.addClass("hidden") : t.removeClass("hidden")
    }))
}

function showContent() {
    $(".fancyLoader").fadeOut(1e3).addClass("hidden")
}

function initLoader(i, n) {
    return i && 0 !== i.length ? void loadContent(i, n) : !1
}

function loadContent(i, n) {
    for (var t = i.length, e = 0, o = 0, h = function() {
            e + o == t && "function" == typeof n && (isContentLoaded = !0, n())
        }, d = function() {
            e++, h()
        }, w = function() {
            o++, h()
        }, s = 0; t > s; s++) {
        var a = new Image;
        a.onload = d, a.onerror = w, a.src = i[s].src
    }
}
var total = 0,
    totalImagesAmount, stripe, windowHeight, windowWidth, percentValueContainer;
$.fn.alignElementsSameHeight = function() {
    $(".same-height-row").each(function() {
        var i = 0,
            n = $(this).find(".same-height");
        n.height("auto"), $(window).width() > 768 && (n.each(function() {
            $(this).innerHeight() > i && (i = $(this).innerHeight())
        }), n.innerHeight(i)), i = 0, n = $(this).find(".same-height-always"), n.height("auto"), n.each(function() {
            $(this).innerHeight() > i && (i = $(this).innerHeight())
        }), n.innerHeight(i)
    })
}, $(window).load(function() {
    windowWidth = $(window).width(), windowHeight = $(window).height(), initLoader($("body img"), showContent), $(this).alignElementsSameHeight(), sticky(), setMobileArrow(windowHeight), setImageEffects()
}), $(window).resize(function() {
    newWindowWidth = $(window).width(), newWindowHeight = $(window).height(), windowWidth && windowWidth !== newWindowWidth && (setTimeout(function() {
        $(this).alignElementsSameHeight()
    }, 100), windowWidth = newWindowWidth, windowHeight = newWindowHeight, setMobileArrow(windowHeight))
});